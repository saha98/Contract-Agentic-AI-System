{
  "name": "contract-ai-system",
  "private": true,
  "scripts": {
    "dev": "npm --prefix gtsm-frontend run dev",
    "backend": "run.bat",
    "build": "npm --prefix gtsm-frontend run build",
    "lint": "npm --prefix gtsm-frontend run lint",
    "preview": "npm --prefix gtsm-frontend run preview"
  }
}
